/* 
    Put a div on the page. When the div is clicked, append the text "mc" to whatever is in its innerHTML. After 3 clicks, the div will show "divdivdiv"
*/

//id the div tag firstep
// use onclick in the div tag
// if clicked then add mc
// after three it will appear as divdivdiv. Instead of mcmcmc

let fries = document.getElementById("fries");





function
ronald()
{
  
        fries.insertAdjacentText("beforeend","div") 
       
        if(fries.innerHTML =="macasdivdivdiv")
        {
            fries.insertAdjacentHTML("afterbegin","macasmcmcmc");
        }
    
}